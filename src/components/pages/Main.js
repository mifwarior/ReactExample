import React from 'react';
import PageTemplate from '../PageTemplate';

const Main = (props)=>{
  console.log(props);
  return (
    <PageTemplate>
      <h1>Main Page</h1>
    </PageTemplate>
  );
}

export default Main;